[build]
  publish = "."

# Proxy API calls to your Express server (avoids CORS issues)
# Replace YOUR_SERVER_URL with your actual server URL
[[redirects]]
  from = "/api/*"
  to = "https://clinyco-ai.onrender.com/api/review/:splat"
  status = 200
  force = true
